/** Public URL of the generated CV (src/app/resume.pdf/route.ts). */
export const RESUME_HREF = '/resume.pdf';

/** Name of the dashboard session cookie. Shared by src/proxy.ts and src/server/auth.ts. */
export const SESSION_COOKIE = 'dashboard_session';
